export component AppWindow inherits Window {
    width: 800px;
    height: 600px;
    title: "Soft Renderer - Alpha Blending";

    in property <image> framebuffer-image;

    Image {
        width: 100%;
        height: 100%;
        source: root.framebuffer-image;
        image-rendering: pixelated;
    }
}
