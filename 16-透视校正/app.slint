export component AppWindow inherits Window {
    preferred-width: 800px;
    preferred-height: 600px;
    title: "Software Renderer - Perspective Correction";

    in property <image> render-image <=> img.source;

    img := Image {
        width: 100%;
        height: 100%;
        image-rendering: pixelated;
    }
}
