export component AppWindow inherits Window {
    in-out property <image> framebuffer;
    title: "Soft Renderer";
    width: 800px;
    height: 600px;

    Image {
        width: 100%;
        height: 100%;
        source: root.framebuffer;
        image-rendering: pixelated;
    }
}
